# Copy to .env and adjust as needed.
#
# GraphQL endpoint. Defaults to a public demo (Countries GraphQL) when unset,
# so the app boots on first install without configuration. Point this at your
# own Yoga GraphQL server to use a real backend.
EXPO_PUBLIC_GRAPHQL_URL=https://countries.trevorblades.com/graphql
